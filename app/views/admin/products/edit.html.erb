<% provide(:title, "商品情報編集") %>
<% provide(:url, admin_product_path(@product)) %>
<div class='container'>
  <div class='row'>
    <div class="col-sm-12 col-md-8 col-lg-5 mx-auto">
      <h2 class="mt-2">商品情報編集</h2>
        <%= render 'form', product: @product %>
    </div>
  </div>
</div>